namespace Assets
{
    /// <summary>
    /// Enum for beaver actions. Used to resolve input to a method 
    /// </summary>
    public enum BeaverBehaviors
    {
        None,
        PickUp,
        Compress,
        Destroy
    }
}